#include "main.h"
/**
 * add - adds two number
 * @num1: first number
 * @num2: second number
 * Return: result
 */
int add(int num1, int num2)
{
	int result;

	result = (num1 + num2);
	return (result);
}
