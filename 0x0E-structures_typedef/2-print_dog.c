#include "dog.h"
#include <stdio.h>
/**
 * print_dog - prints the members of struct dog
 * @d: pointer to stuct dog
 */
void print_dog(struct dog *d)
{
	if (d == NULL)
		printf(" \n");
	else
		name = d -> name;
		age = d -> age;
		owner = d -> owner;

